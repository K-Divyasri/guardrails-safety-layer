# Core - needed for the guards to run at all.
pydantic>=2.0

# The web app itself. (Streamlit already depends on pandas, which app.py needs
# for the red-team results table, so it's pulled in automatically.)
streamlit>=1.30

# Optional - only used if you tick "use LLM classifier" in the sidebar.
litellm>=1.40
python-dotenv>=1.0
